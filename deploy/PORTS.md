# 端口约定

| 用途 | 地址 | 公网 |
|---|---|---|
| 后台 HTTP/WebSocket 容器 | `guardian-api:3000` | 否 |
| 宝塔反代上游 | `127.0.0.1:18080` | 否，仅本机 |
| 宝塔 HTTPS | `443` | 是 |
| 现有 sub2api | `8384` | 保持不变 |

请先执行 `ss -lntp` 和 `docker ps` 再部署；脚本不会修改或停止已有容器。
